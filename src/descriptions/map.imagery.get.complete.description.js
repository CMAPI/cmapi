cmapi.channel["map.imagery.get.complete"].description = {
    "description": "Schema for the details object for a map.message.complete message after a map.imagery.get.",
    "properties": {
        imageries: {
            "description": "List of imagery data. The format of data is same with map.imagery.create"
        }
    }
};
